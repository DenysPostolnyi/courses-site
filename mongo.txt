$lookup
1) в какой таблице искать совпадение
2) значение из данной таблицы
3) значение из другой таблицы
4) массив в который запишется срока в кооторой нашлось совпадение из другой таблицы ('r1')
$unwind - покажет только объекты в которых нашлось совпадение
1) путь к записанному массиву ('$r1')
2) ('r1_id')
3) (false)
$match 
	'user_id': 1
выведет объект пользователя с айди 1